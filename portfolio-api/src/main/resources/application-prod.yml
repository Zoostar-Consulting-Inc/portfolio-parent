logging:
  level:
    root: info
    '[org.springframework.orm.jpa]': debug
    '[com.zoostarinc]': debug
    '[net.zoostar]': debug

spring:
  datasource:
    url: mongodb+srv://pratik_db_user:${db.portfolio.password}@cluster0.vy1xwq4.mongodb.net
    driver-class-name: org.h2.Driver
    username: pratik_db_user
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: false
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
